import React from 'react'
import { ListenClientProps } from '../types'

const ListingClient = ({currentUser, listing}:ListenClientProps) => {
  return (
    <div>ListingClient</div>
  )
}

export default ListingClient